# PanSystem Frontend

Vue 3 + TypeScript + Vite frontend for `PanSystem`.

## Commands

```bash
npm install
npm run dev
npm run build
```

Default dev API base URL is `https://localhost:7296/api` in `src/utils/request.ts`.
